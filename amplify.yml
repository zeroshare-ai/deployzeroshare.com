version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Static HTML deployment"
    build:
      commands:
        - echo "Serving static files"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
      - '!.git/**'
      - '!node_modules/**'
      - '!.next/**'
      - '!out/**'
