/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Amplify supports serverless Next.js by default
  // Remove standalone output for Amplify compatibility
}

module.exports = nextConfig
